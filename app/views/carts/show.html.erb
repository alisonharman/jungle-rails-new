<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  <%if !enhanced_cart.empty? %>
   <%= render "panel" %>
  <%  else %>
   <%= link_to 'Cart is empty!', :root %>
<% end %>

  

</section>
